const multiploOcho = 8;
const multiplos = [];
let i = 1;

while (i <= 500) {
    const multiplicacion = i * multiploOcho;
    multiplos.push(multiplicacion);
    i++;
}

alert(`MUltiplos: ${multiplos}`); 